# Este app não define modelos de banco de dados de propósito.
#
# As "notas" são apenas dicionários Python guardados na sessão do
# usuário (ver notas/services.py e notas/views.py). Isso elimina a
# necessidade de banco de dados e migrações no deploy — o foco da aula
# é o pipeline de CI/CD, não a modelagem de dados.
